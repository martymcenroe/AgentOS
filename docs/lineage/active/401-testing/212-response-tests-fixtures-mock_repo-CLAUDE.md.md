```markdown
# CLAUDE.md - Mock Project

## Overview

This is a mock project used for testing codebase analysis.

## Coding Conventions

- Use snake_case for all module and function names
- Use PascalCase for class names
- All public functions must have docstrings
- Maximum line length is 100 characters

## Rules

- Always use `poetry run python` for execution
- Tests must use pytest with fixtures
- No wildcard imports in production code

## Project Structure

The source code lives in `src/` and tests in `tests/`.
```
