# Test Claude -p Integration

This is a simple test brief to verify the claude -p integration works.

## Feature Request
Add a simple utility function that prints "Hello, World!" to the console.

## Acceptance Criteria
- Function should be named `hello_world()`
- Should print to stdout
- No parameters required
